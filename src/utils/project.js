export const createScene = (index=1) => ({ id:crypto.randomUUID(), name:`Scene ${index}`, image:'', imageName:'', duration:5, voice:'', voiceName:'', voiceDuration:0, dialogue:'', volume:1, transition:'cut', transitionDuration:0.5, zoom:'none', panX:0, panY:0, rotation:0, shake:false, subtitle:{fontSize:32, position:'bottom', background:true, fade:true} });
export const createProjectSnapshot = (project) => ({ version:1, name:project.name, settings:project.settings, scenes:project.scenes.map(({image,voice,...s})=>s), music: project.music ? {...project.music, url:undefined} : null, characters:project.characters });
export const downloadJson = (obj, filename='anime-project.json') => { const blob=new Blob([JSON.stringify(obj,null,2)],{type:'application/json'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=filename; a.click(); setTimeout(()=>URL.revokeObjectURL(a.href),1000); };
