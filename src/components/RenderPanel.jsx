export default function RenderPanel({status,progress,error,downloadUrl,onDownload,onClose}){if(!status&&!error&&!downloadUrl)return null;return <div className="modal-backdrop"><div className="render-modal"><div className="render-title"><div><h2>{error?'Render failed':downloadUrl?'Render complete':'Rendering project'}</h2><p>{error||status}</p></div>{(downloadUrl||error)&&<button className="icon" onClick={onClose}>×</button>}</div>{!error&&!downloadUrl&&<><div className="progress"><div style={{width:`${progress}%`}}/></div><div className="progress-num">{Math.round(progress)}%</div></>}{downloadUrl&&!error&&<button className="download-big" onClick={onDownload}>DOWNLOAD MP4</button>}{error&&<p className="error-box">{error}</p>}<small className="render-note">Rendering happens locally in this browser. Long or high-resolution projects may use substantial RAM.</small></div></div>}
